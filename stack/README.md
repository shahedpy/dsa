# Stack
**Basic Operations on Stack:**  
In order to make manipulations in a stack, there are certain operations provided to us.
- **push():** to insert an element into the stack
- **pop():** to remove an element from the stack
- **top():** Returns the top element of the stack.
- **isEmpty():** returns `true` if stack is empty else `false`.
- **isFull():** returns `true` if the stack is full else `false`.
  
[Simple Stack Visualization](https://www.cs.usfca.edu/~galles/visualization/SimpleStack.html)

### Use Cases of Stack
1. Function call management (call stack).
2. Undo operations in text editors.
3. Balancing of parentheses in expressions.
4. Depth-first search (DFS) in graphs.
5. Evaluating and converting expressions (e.g., infix to postfix).
